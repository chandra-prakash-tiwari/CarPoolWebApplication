import * as React from 'react';
import { Dialog } from '@material-ui/core';

export default class RideBooking extends React.Component {
    render() {
        return (
            <Dialog fullScreen open={true} >
            </Dialog>
            )
    }
}